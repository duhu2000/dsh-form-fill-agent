# Changelog
## [0.2.15] - 2026-09-08

- 改善搜索元数据、安装说明、能力边界与四产品互链；无运行时或依赖变更。


## 0.2.14

- 应用有效字段配置后直接进入主体核验。
- 校验错误使用红色提示框，重复映射同时标记冲突列及修正指引。

## 0.2.13

- 选择 XLSX 后自动解析，取消额外解析按钮。
- 多义字段候选直接展示，地址优先推荐注册地址、通信地址、开票地址。
- 保留 0.2.12 共享实际控制人字段能力。

## 0.2.12

- 接入 qcc-field-contracts 0.1.0，共享实际控制人名称及三项持股/表决权比例，目录增至 133 字段，支持长表头别名推荐与原列补空。
- 多名控制人、分页或异常返回不默认选第一人；字段级核验原因显示于未完成清单，保护其他有效字段及原有非空值。
- 共享内核升级 0.2.9，Provider 升级 0.2.1；保持已有字段设置冲突修复及任务 schema 不变。

## 0.2.11

- 预览页底部常驻蓝色“下一步：确认下载”，自动保存当前单元格选择后定位生成副本按钮。
- 生成填写指令使用主按钮，填写范围确认使用蓝色提示区；未纳入字段改为折叠明细。
- 刷新与删除收进“更多任务操作”；浏览器回归覆盖窄侧栏按钮可见及新副本下载。

## 0.2.10

- 应用字段设置后先检查可执行范围，缺少表头、主体或填写字段时显示具体纠正指引，不再错误显示下一步成功。
- 重复字段映射提示冲突的目标字段；生成指令被拦截时返回字段设置，保留已有选择供修正。
- 浏览器回归覆盖冲突保存、阻止无效跳转、修正后直接生成指令，保持内核 0.2.8 和 Provider 0.2.0。

## 0.2.9

- 提示词向导按企查查蓝规范改为数字圆标四步导航，手机两列布局，统一标题、上传区和固定底部操作。
- 正向说明企查查 MCP 的企业核验与原表填写能力，替换“不调用企查查”主提示，保留发送后执行与草稿编辑流程。
- 32 组深浅色与窗口布局验证通过，内核保持 0.2.8，Provider 0.2.0。

## 0.2.8

- 支持标准 duplicateValues 条件格式并保留原 XML 和样式。
- 接受任意值校验；静态整数 between 校验保留但该区域禁止自动填写，其他未知结构继续拦截。
- 主包和共享内核 0.2.8，Provider 保持 0.2.0。

## 0.2.7

- 分析成功后默认展开完整原表和结果网格。
- 主包取消隐式 100 次调用上限；DSH_FORM_FILL_MAX_CALLS 可设正整数，未设置或 0 不限本地调用次数。
- 本地限额提示包含预计量、上限及配置来源，明确不是企查查余额；共享内核的直接调用默认不变。

## 0.2.6

- 工作台生成填写指令直接回填对话框，使用服务端已确认字段范围，不重复弹出任务向导。
- 未纳入列在工作台内确认；保留主体缺失检查、手写草稿冲突保护和独立页面复制降级。
- 提示词生成入口的字段列表仅显示原表映射字段，保留任务范围保存与预览下载流程。

## 0.2.5

- 字段列表双击、Enter、确认按钮可确认并收起；推荐选择后自动收起并恢复焦点。
- 应用字段设置成功后展示操作说明及“下一步：主体核验”，可继续生成填写指令。
- 字段再次修改后须重新应用设置，保留原表范围与查询确认流程。

## 0.2.4

- 每列字段选择器集中展示推荐、关键词搜索、维度过滤及分组列表，支持键盘选择和 Escape 收起。
- 补齐地址、网址等常见表头推荐，保留精确自动匹配和人工确认。
- 保持仅填写原表映射字段；共享内核、Provider 及任务 schema 不变。

## 0.2.3

- 字段映射增加关键词模糊搜索、维度筛选和显式推荐，保留筛选外的当前选择。
- 映射变更后刷新向导范围，避免旧草稿字段选择遗漏；缺少主体锚点时阻止执行，排除字段须确认。
- 预览按字段统计填写与未完成项，区分缺少锚点、未映射、无数据和调用失败；生成副本不再暗示全部完成。
- 增加七列合成表完整填写及选择恢复回归；共享内核和 Provider 保持 0.2.0。

## 0.2.2

- 工作台支持左边缘连续拖拽、键盘微调、双击复位及取消拖动，展开后可恢复手动宽度。
- 按宿主可用空间约束宽度、中央会话同步让位，窄屏降级全屏；关闭重开保留当前客户端宽度偏好。
- 通过 GitHub Actions OIDC 自动发布；内核及 Provider 保持 0.2.0。

## 0.2.1

- 首页输入框下方快捷入口改为独立描边卡片，单行排列、上图标下短标签，窄屏仅菜单内部横向滚动。
- 右侧工作台统一紧凑标题栏、浅灰背景和白色内容分区，去除重复标题与空任务控制区，统一上传控件。
- 八组首页与十组工作台深浅色/宽度布局验证及既有交互回归通过；内核与 Provider 保持 0.2.0。

## 0.2.0

- 首个正式编号版本，三包统一 0.2.0，主包精确依赖同版内核和 Provider。
- 汇总 alpha.11 已交付的 XLSX 填写、字段映射、恢复选择、任务取消/重试、受限 Excel 保真及企查查蓝工作台。
- 保留 alpha.10 的 ZIP 目录、自定义属性和计算元数据导入兼容修复。
- 统一运行时版本标识与发布文档；任务存储 schema 5、核心 schema 1 保持不变。
- 非技术用户签收、Microsoft Excel 桌面验收及市场维护者合并仍独立跟踪，不以正式编号替代验收。

## 0.1.0-alpha.11

- 工作台阶段菜单统一为招投标式等宽分栏、上图标下短标题，去掉数字前缀，说明留在正文。
- 选中态使用浅蓝底、蓝字与底部蓝线；窄屏缩小图标字号，单行省略，避免横向越界。
- 十组宽度/深浅色布局与现有配置交互回归通过；内核保持 alpha.10，Provider 保持 alpha.9。

## 0.1.0-alpha.10

- 修复正常 XLSX 空目录条目被误判为不安全 ZIP 路径，继续拦截重复、穿越及文件/目录冲突。
- 保留自定义文档属性和严格验证的 Excel calcFeatures 元数据，其他未知扩展仍拒绝。
- 更新导入页支持范围说明；新增合成文件预览、写出保真及恶意结构回归。
- 主包与内核 alpha.10；Provider 无改动，保持 alpha.9。

## 0.1.0-alpha.9

- 完成原 128 字段目录及七组非工商投影迁移，抽取前 21 项 golden 保持 parity。
- 显式适配真实服务新增的关联惩戒名单维度，目录共 129 字段；其他未知漂移失败关闭。
- 运行时按工具可用性启用企业、经营和风控能力，先核验身份，再读取选中字段；无工具时可降级。
- 支持仅信用代码的备用锚点与旧字段名别名；补齐宽列合成 fixture。
- 三包 alpha.9，npm 按所有者要求延后；详见 docs/U6-ACCEPTANCE.md。

## 0.1.0-alpha.8

- 完整原表/结果网格、分页搜索、可恢复单元格选择及字段规则复用。
- 逐调用持久检查点、运行取消、仅失败或未执行部分重试；任务 schema 5 兼容读取 1–5。
- 工商 26 字段与显式候选检索；按最坏调用数预算，不自动选首项。
- 支持普通内部公式、固定区域/命名区域下拉、基础条件格式和普通表格对象。
- 最新清洗 0.8.8 tarball parity、双插件同装和大表性能回归。
- 三包同版；npm 暂不发布，详见 docs/U5-ACCEPTANCE.md。

## 0.1.0-alpha.7

- 修复侧栏 AI填表入口多余左缩进，统一图标、文字与相邻菜单的左对齐。
- 仅主包样式修复；共享内核与 Provider 保持 alpha.5。

## 0.1.0-alpha.6

- 按共享交互规范 v1.1.0 接入企查查蓝深浅色、线性图标、左上入口与专属首页横排品牌。
- 四步提示词向导支持字段搜索、选择及恢复；已有草稿可取消、替换或追加，重复回填不重复追加。
- 字段范围实际约束查询/填写，schema 4 持久化并继续读取旧任务。
- 工作台增加字段/结果筛选、完整下载说明及全窗居中向导；普通会话恢复宿主品牌。
- 仅主包升级 alpha.6；共享内核、Provider 保持 alpha.5，详见 docs/U4-UI-ACCEPTANCE.md。

## 0.1.0-alpha.5

- 工作表、表头行、字段映射人工修正，变更后重新生成预览。
- 歧义主体候选显式选择或人工补充，真实任务需再次精确查询，不覆盖原表主体名。
- 支持固定文本选项下拉列表原样保留；不在选项内的值不写入。
- schema 3 持久化配置与选择；读取旧任务，回退需独立目录。
- 三个包同步升级；59 项测试与双消费者、新增 UI 验收通过，完整边界见 U3-CONTROLS-ACCEPTANCE.md。

## 0.1.0-alpha.4

- 补齐实际 npm 子包 README，提供简介、安装、使用流程、能力边界与文档入口。
- 打包门禁要求智能体 tarball 包含 README 和安装说明，防止 monorepo 根文档未进入子包。
- 仅文档和发布检查调整，运行代码及共享依赖版本保持不变。

## 0.1.0-alpha.3 — 2026-09-06

- 修复缺少 package.json 导出导致真实 DSH 未发现原生客户端的问题。
- 修正客户端模块依赖声明，运行时服务依赖与模块依赖分开。
- 修复真实对话容器未预留右栏空间、已完成任务恢复覆盖导航的问题。
- 运行时探测 alpha.2 的 uiWorkspace，新建普通会话不复用 AI填表业务会话；rc.2 保持兼容。
- 新增两版真实原生浏览器回归；U2 真实模型 → QCC 六字段 → 自动刷新预览 → 确认下载闭环通过。

## 0.1.0-alpha.2 — 2026-09-06

- DSH 专属业务会话、输入框下导航、可关闭右侧工作台；普通新会话保持独立。
- 五步工作台、居中指令弹窗、回填原生草稿、任务自动刷新与会话内恢复。
- 真实文件上传先做本地分析；合成演示独立折叠展示。
- 本浏览器访问凭据限定的任务历史、revision 冲突保护、单元格排除与确认下载。
- 任务 schema 2 元数据、schema 1 读取迁移、查询中断恢复与失败状态。
- core / Provider 保持 0.1.0-alpha.1；范围及验收见 docs/U1-ACCEPTANCE.md。

## 0.1.0-alpha.1 — 2026-09-06

- M0 golden 与双消费者 tarball 契约。
- M1 XLSX 安全解析、单元格待填识别、mock Provider、预览、确认后写回副本与未完成清单。
- M2 真实企查查工商 Provider、Agent-owned 高层工具及真实调用写回验证。
- 任务磁盘保存、重启恢复、确认结果重建、过期清理与删除。
- 双版隔离 DSH 浏览器验收；发布渠道状态见 docs/M2-ACCEPTANCE.md。
