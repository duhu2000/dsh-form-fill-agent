# 0.2.25

Better Sidebar 作为可选工作台依赖：基础安装不再强装侧栏，预检默认 basic 模式允许缺少侧栏；workbench 模式检查工作台前置条件。已安装但与宿主不兼容的侧栏/context 仍阻断。

无侧栏时，工作台按钮明确提示并提供现有独立填表页面入口，保留原生草稿。独立页面按当前会话恢复最近已保存任务，支持上传、映射、预览、确认和导出；自动回填原生输入框仍需内嵌工作台，独立页面需复制指令后手动发送。未迁移原生容器，未更新共享包。

验证：124 项测试、四包打包检查和 macOS/Windows/Linux × Node22/24 六组候选 CI 通过；真实隔离宿主0.1.2-rc.1无侧栏及sidebar0.18.1下合成XLSX业务通过，真实Host Agent/tools管道通过合成QCC一次查询生成一格预览。旧宿主0.1.1-rc.2无侧栏基础回归通过。最终打包预检阻断新旧侧栏错误配对。正式发布再运行六组CI和registry安装回归。

真实模型/计费MCP、四产品共存及全部浮窗/底部组合尚未验收；未改动生产DSH或全局宿主。详见 OPTIONAL-SIDEBAR-ACCEPTANCE.md。
