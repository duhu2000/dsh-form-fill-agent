# 0.2.20

接入受益所有人首条和财务首个返回报告期字段。受益人、主营业务收入及带「重复列 N」后缀的原列提供推荐；多义字段仍需人工采用，并明确首条及收入统计口径差异。缺失值不跨记录或报告期补取，保留原值及来源报告期。

取消未纳入列的重复勾选门槛，保留范围摘要与用户发送机制。回填后进入主体核验，执行期间显示查询状态，结束后进入填写预览。

主包 0.2.20 精确依赖 Provider 0.2.2；core 0.2.10、字段契约 0.1.0 不变。目录共 137 字段。首条投影暂作为 Provider 内部模块，后续共享包统一时可收敛。

117 项测试及打包检查、候选/配置/原生 React Host 浏览器回归通过。延迟合成 Provider 验证进度切换；未声称真实 DSH 模型或真实企查查端到端已验收。发布分支 release/0.2.20。
