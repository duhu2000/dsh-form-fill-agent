# 0.2.19

修复字段搜索与推荐别名不一致：向导和工作台均可使用推荐规则中的英文别名及多义词搜索，筛选不会清除原有映射。

隐藏空的未完成项卡片和下载入口。查询进度明确按计划项统计，包含失败，不等于填写成功；工具结果说明不得仅凭可填写为 0 推断授权异常，需依据具体原因记录。

验证：115 项测试、四包检查、合成表格浏览器回归通过，覆盖别名搜索、零匹配保留选择、候选确认、刷新恢复、深浅色窄屏和空清单入口。

仅发布主包 0.2.19；core 0.2.10、Provider 0.2.1、字段契约 0.1.0 不变。发布分支 release/0.2.19，保留主线未集成的共享字段包开发。本轮未进行真实企查查调用或用户 DSH 重启。
