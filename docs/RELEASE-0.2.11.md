# 0.2.11

明确填写任务的下一步：生成指令使用蓝色主按钮；范围确认单独突出显示，长篇未纳入字段默认收起。预览页底部固定“下一步：确认下载”，保存当前勾选后跳转并定位“确认这些填写，生成新副本”。确认仍由用户操作，原表不覆盖。

刷新和删除收进更多任务操作，避免与主流程竞争。

验证：103 项测试与三个包打包检查通过；浏览器回归覆盖指令生成、预览选择、确认和下载，补验 390px 窄侧栏按钮不超界。内核 0.2.8、Provider 0.2.0 未变更。
