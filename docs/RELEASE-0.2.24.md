# 0.2.24

新增只读安装兼容预检 `form-fill-preflight`，检查实际 DSH、Node、侧栏与可选 context 版本，已知不兼容组合明确阻断，未知组合明确提示尚未验证。不存在的 profile 路径会报错。安装文档按宿主给出匹配版本，避免无条件升级侧栏 latest。

安装回归使用隔离 HOME/profile，覆盖真实 tarball 的上传、映射、三组合成 XLSX 预览与导出、草稿回填、新会话、侧栏关闭恢复，并自动清理临时依赖。

验证：123 项测试及四包打包检查通过；候选提交通过 macOS/Windows/Linux × Node 22/24 六组 CI。隔离 DSH 0.1.1-rc.2 + sidebar 0.17.1，以及 DSH 0.1.2-rc.1 + sidebar 0.18.1 + context 0.48.0 的浏览器回归通过，页面错误为 0。发布提交再执行相同 CI。

仅主包从 0.2.23 更新到 0.2.24；共享字段、核心及 provider 版本不变。不修改生产 DSH，不执行真实模型或 MCP 查询。四产品共同集成及全部浮窗/底部组合仍待协调验收；版本预检通过不代表业务查询与权限已验证。
