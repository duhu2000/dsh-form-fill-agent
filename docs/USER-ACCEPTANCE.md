# AI填表 alpha.11 用户验收清单

本清单供非技术用户和 Excel 桌面使用者签收，当前未代签。只使用三套合成模板或自有获授权测试表格。

验收基线：dsh-form-fill-agent 0.1.0-alpha.11、form-fill-core 0.1.0-alpha.10、qcc-form-fill-provider 0.1.0-alpha.9。菜单为「导入表格 / 字段设置 / 主体核验 / 填写预览 / 确认下载」。自动化通过不替代本人签收。

| 场景 | 操作与期望 | 签收 |
|---|---|---|
| 首次使用 | 左上 AI填表入口、首页品牌正确；普通会话恢复宿主品牌 | 待用户 |
| 两插件同装 | 分别输入手写草稿；切换后不串入另一个工作台或草稿 | 待用户 |
| 字段设置 | 为未知表头指定字段；保存规则，在相同表头文件显式复用；不同表头拒绝套用 | 待用户 |
| 原表与结果 | 查看后续行和列，搜索位于最后一列的内容；切换原表/结果，原值可对照 | 待用户 |
| 选择 | 排除一格，再恢复；重复调整和刷新，已选格一致；筛选不会改变选择 | 待用户 |
| 主体候选 | 简称检索显示候选但不自动选择；确认后重新核验，不覆盖原表主体名称 | 待用户 |
| 取消与重试 | 中途取消，已成功结果保留；仅重试未完成项；重新打开页面能恢复进度 | 待用户 |
| 保真 | 用 Excel 打开新副本，无修复警告；公式重算正确，下拉、条件格式、表格样式保留 | 待 Excel 桌面 |
| 已有内容 | 公式、零值、已有文本及受保护来源区不填写；原文件不变 | 待用户 |
| 小屏/键盘 | 深浅色、1440/1024/390px、低高度窗口均可完成向导和下载；Esc 可关闭向导 | 待用户 |
| 异常 | 断网后显示失败，重新查询不丢成功值；任务过期/删除后不可下载 | 待用户 |

## 本人验收记录

验收人：待填写；日期：待填写；系统及 DSH 版本：待填写；Excel 版本：待填写。

逐项填写「通过 / 未通过 / 未执行」、实际结果和合成复现方式。问题修复后记录复验版本，不能直接沿用旧版本结论。建议先用客户台账完成全流程，再用供应商准入表和合同主体信息表复验映射与原值保留。

2026-09-07 环境检查：本机 /Applications 未发现 Microsoft Excel，存在 LibreOffice 和 WPS。两者的打开结果不能替代 Microsoft Excel 桌面签收；不自动购买或安装 Office。

记录版本、系统/Excel 版本、场景、实际结果及最小合成复现文件。不要提交客户名单、Token 或工具原始响应。完成操作并明确签收后，才能将“正式用户验收”改为完成。
