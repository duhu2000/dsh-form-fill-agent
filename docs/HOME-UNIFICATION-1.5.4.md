# DSH-UX-001 v1.5.4 首页统一采用记录

日期：2026-09-14。基线为远端 main 425726b4e759caa6bd08c460c2b9dd741f3c5a7f（已只读核验），独立分支 fix/home-unification-20260914。未修改原工作树、生产 DSH Profile、全局宿主、版本号或 Tag。

## 已实施

- UX-05：删除首页用途副标题 DOM 与对应段落 CSS；保留图标和完整标题、宿主真实工作区/模式选择、原生输入和提示词生成、输入框外的快捷菜单顺序。
- UX-45：固定左侧名称“AI 填表”、首页“AI 填表智能体”。快捷入口仍为导入表格、字段设置、主体核验、填写预览、任务历史；业务步骤不照搬其他产品。既有 Session 创建 promise 去重保持不变，状态不替换入口名称。
- UX-46：首页品牌适配仅对本产品显式 Session 前缀和原生 Hero 欢迎标题生效。以 Session 订阅和会话区域内 MutationObserver 替换 200ms 轮询；Host 欢迎子树重挂载可恢复，普通会话/其他业务/自定义标题恢复宿主内容，卸载清理订阅、observer 与自有节点。不调用 Session rename、不改保存的历史标题。旧基线缺少品牌槽位，仍采用集中在 Hero 组件内的可撤销 DOM 桥接。
- UX-47：复用 0.2.28 实际任务进度，不新增首页常驻说明。结束态区分等待核验确认、已停止未完成、确认后已完成；轮询失败显示状态同步异常且保留现有进度，下一次成功同步恢复真实任务状态。不发起新查询或制造进度。

## 验证

- npm test：131 passed。
- scripts/verify-pack.mjs：四包验证通过（候选沿用 0.2.28 只用于本地验收，禁止覆盖已发布版本）。
- scripts/home-ui-smoke.mjs：隔离 React 宿主契约浏览器验证通过。包括深浅色 × 390/1440 宽度、标题/原生选项/输入/菜单顺序、无副标题、草稿保护、宿主标题子树替换、自定义标题、业务/其他/普通切换、刷新、卸载订阅清理，以及四种任务状态与同步失败保留进度。
- 对应已有 Host/UI smoke 的品牌文字断言已同步新名称。

本轮浏览器验证为合成 React Host 契约；没有声称完整真实 DSH、真实模型、计费 MCP 或四插件共存通过。真实目标宿主最终包验证和跨产品共存由协调验收继续完成。实现不需要强制安装 Better Sidebar，也未改变无侧栏独立填表页面能力。
