# 0.2.10

修复字段设置已保存但无法生成指令时缺少明确原因的问题。应用设置即检查任务可执行性；冲突、缺少表头或填写字段时显示纠正指引，生成指令被阻止时返回字段设置。设置与原文件均保留，修正后可直接生成指令。

本次只更新主包，内核 0.2.8、Provider 0.2.0 保持不变。

本地验证：npm run check 103 项测试通过、三个包打包检查通过；configuration-ui-smoke 浏览器回归通过，包含重复映射修正后生成指令及 32 组向导布局。

用户原始样表可识别第 4 行表头及信用代码待填格。本次未取得截图对应任务的运行内存，不能确定用户保存的具体冲突；未修改生产配置或执行真实查询。发布和安装结果待远端流程完成后补录。
