# 安全边界

仓库测试只使用合成 fixture。禁止提交凭据、原始工具响应及真实企业名单。真实 Provider 通过调用方授权的 MCP transport 执行；DSH 中只允许 Agent-owned 高层工具嵌套调用。网页不直接触发真实 MCP。任务磁盘存储只写原输入和归一化预览，目录 0700、文件 0600；请使用仓库外专用目录。生产数据不得提交为测试 fixture。

默认只填空白，生成新文件；支持矩阵见 docs/security.md。测试使用隔离 DSH_HOME、合成数据和非 43120 端口。真实调用和发布已获项目所有者授权，不将该授权误用为其他用户的账户授权。
